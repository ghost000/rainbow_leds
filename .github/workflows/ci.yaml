name: CI
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - "dev/*"

jobs:
  # flutter_test:
  #   name: Run flutter test and analyze
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-java@v1
  #       with:
  #         java-version: "12.x"
  #     - uses: subosito/flutter-action@v1
  #       with:
  #         channel: "stable"
  #     - run: flutter pub get
  #     - run: flutter analyze
  #     #- run: flutter test
  #     - run: flutter build apk

  # build_ios:
  #   name: Build Flutter (iOS)
  #   needs: [flutter_test]
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-java@v1
  #       with:
  #         java-version: "12.x"
  #     - uses: subosito/flutter-action@v1
  #       with:
  #         channel: "stable"
  #     - run: flutter pub get
  #     - run: flutter clean
  #     - run: flutter build ios

  # build_appbundle:
  #   name: Build Flutter (Android)
  #   needs: [flutter_test]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-java@v1
  #       with:
  #         java-version: "12.x"
  #     - uses: subosito/flutter-action@v1
  #       with:
  #         channel: "stable"
  #     - run: flutter pub get
  #     - run: flutter clean
  #     - run: flutter build appbundle

  build_app_and_save_apk_to_artifacts:
    name: Build APK and save to artifacts (Android)
    # needs: [flutter_test, build_appbundle]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "17.x"
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: '2.5.3'
          channel: "stable"
      - run: flutter pub get
      - run: flutter clean
      - run: flutter analyze
      - run: flutter test -v
      - run: flutter clean
      # - run: flutter build apk --split-per-abi
      # - name: Upload APK
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: app
      #     path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
